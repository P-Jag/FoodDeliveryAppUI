import 'package:flutter/material.dart';

const kMainPadding = EdgeInsets.symmetric(horizontal: 20.0);
const kMainContainerMargin = EdgeInsets.all(12.0);
final kMainTilesBorderRadius = BorderRadius.circular(15.0);

const kMainTextStyle = TextStyle(
  fontSize: 24.0,
  fontWeight: FontWeight.w600,
  letterSpacing: 1.2,
);

const kSecondaryTileText = TextStyle(
  fontSize: 16.0,
  fontWeight: FontWeight.w600,
);

final kMainBoxDecoration = BoxDecoration(
  color: Colors.white,
  borderRadius: BorderRadius.circular(15.0),
  border: Border.all(
    color: Colors.grey[300],
    width: 1.0,
  ),
);
